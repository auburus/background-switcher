---
name: CI
on: [push, pull-request]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: .
      - name: Setenv
        run: make setenv
      - name: Lint
        run: make lint
